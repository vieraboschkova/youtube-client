import { BottomColorDirective } from './bottom-color.directive';

describe('BottomColorDirective', () => {
  it('should create an instance', () => {
    const directive = new BottomColorDirective();
    expect(directive).toBeTruthy();
  });
});
